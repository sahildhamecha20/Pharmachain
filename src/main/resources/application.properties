spring.application.name=PharmaChain

# Database Configuration (Using Variables for Security)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/pharmachain_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:S@hilsd2004}

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# Server Port
server.port=${PORT:8080}